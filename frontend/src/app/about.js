export default function About() {
  return (
    <div>
      <h1>About Page</h1>
      {/* {articles.map(article => (
        <article key={article.id}>
          <h2>{article.title}</h2>
          <p>{article.description}</p>
        </article>
      ))} */}
    </div>
  );
}
